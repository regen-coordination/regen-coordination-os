{
  "name": "coop",
  "version": "0.1.0",
  "private": true,
  "description": "Browser-based knowledge commons monorepo for community coordination.",
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "check": "turbo run check",
    "format": "biome format . --write"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "turbo": "^2.1.3",
    "typescript": "^5.9.3"
  }
}
